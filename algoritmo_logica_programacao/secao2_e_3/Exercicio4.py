#entradas
salario_hora = float(input("Informe o salário hora:"))
horas_trabalhadas = float(input("Informe a quantidade de horas trabalhadas:"))

#processamento
salario_total = salario_hora * horas_trabalhadas

#saída
print("O salário total é {0:.2f}".format(salario_total))
